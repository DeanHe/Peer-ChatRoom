package DPCCore;

/**
 * @author Georgi Simeonov
 * Place to keep all the constant as an effort to limit random magical numbers (ports for example)
 */
public final class DPCConstants {
    public static int MASTER_SERVER_PORT = 1977;
enum State {NOT_CONNECTED, APPROVED_FOR_CONNECTION, CONNECTED, ELECTION_STATE}
}
